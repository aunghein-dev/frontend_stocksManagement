export type RefundModelData = {
  oldTranId: number,
  groupName: string,
  oldQty: number,
  itemId: number,
};

export type StockDeleteModelData = {
  itemId: string,
  groupName: string,
  groupId: number
}
